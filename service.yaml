apiVersion: v1
kind: Service
metadata:
  name: gitops-app
  namespace: production
spec:
  selector:
    app: gitops-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
